# 归档.md — 已完成工单与历史验收存档（只读，勿执行）

> 本文件是 DIRECTOR.md 的归档区：**已完成/已验收/已判死的工单全文、验收记录、
> 安全审计裁决、历史状态沉淀、施工回报存档**迁入此处，保留原貌以便复盘与防翻案。
> pi 的施工指令一律看 [DIRECTOR.md](DIRECTOR.md)（只留待施工项），本文件不执行任何工单。
> 本次归档：2026-09-11（DIRECTOR.md 瘦身，工单区只留待施工项；功能沉淀并入总监.md；
> BUILDER.md 已完结回报迁入第九节）

## 目录

- [一、已归档工单（禁止重复执行）](#一已归档工单全部禁止重复执行)
- [二、验收记录（review 历史）](#二验收记录review-历史)
- [三、安全审计裁决记录](#三安全审计裁决记录)
- [四、历史状态沉淀](#四历史状态沉淀)
- [五、与 AGENTS.md 的交接查证](#五与-agentsmd-的交接查证)
- [六、pi 请示裁决（史条目存档）](#六pi-请示裁决史条目存档)
- [七、当前未结事项（账目）](#七当前未结事项账目)
- [八、后续排队（未签发）](#八后续排队未签发)
- [九、施工回报存档（BUILDER.md 迁入）](#九施工回报存档buildermd-迁入)


## 一、已归档工单（全部禁止重复执行）

> 以下工单自 DIRECTOR.md 原样迁入，标题状态标记保留。判死单（工单八）完整留档
> 供「下次想做同类功能前先重读」；部分完成单（工单二）与其收口单（工单二b）合并归档。

### 工单一：消息协议建型 —— ✅ 已完成已验收（f278f2f，勿重复执行）

新建 `src/protocol.ts`，给全部跨边界消息建判别联合类型：

- `WebviewToHost`：webview→宿主消息全集。以 [src/panel.ts](src/panel.ts) `onWebviewMessage` 的
  case 为准：webviewReady / prompt / abort / retryFromLast / pickSession / treeFork / importSession /
  shareSession / newSession / uploadImage / attachFile / pickMode / getSlash / listSessions /
  openSession / revealSessionFile / openPath / deleteSession / getFiles / more / settings /
  pickModel / pickThinking / pickTheme / pickLang
- `HostToWebview`：宿主→webview 消息全集。以 [webview/main.ts](webview/main.ts) 的 message
  监听分发给为准：user / newLive / delta / thinking / toolCallStart / toolCallDelta / toolStart /
  toolEnd / busy / render / queue / notice / fillInput / status / mode / queuedAdd /
  queuedDelivered / queuedClear / codeCtx / addImages / addFiles / slashList / fileList / state / theme
- `PiEvent`：pi RPC 协议事件。以 [src/piClient.ts](src/piClient.ts) 转发 + panel.ts `onPiEvent`
  的 case 为准：agent_start / message_start / message_update / tool_execution_start /
  tool_execution_end / model_select / thinking_level_select / auto_retry_start / auto_retry_end /
  queue_update / extension_error / agent_settled，外加 default 分支的兜底形状
- 接线要求：
  - panel.ts：`post(msg: HostToWebview)`、`onWebviewMessage(m: WebviewToHost)`、`onPiEvent(e: PiEvent)`
  - webview/main.ts 导入类型（注意 vite tsconfig 需能解析 `../src/protocol`；若路径不便，
    可把 protocol.ts 放 `src/` 与 `webview/` 之外的共享位置，或用相对路径 type-only import）
  - 目标：两条消息链路上 `m: any` 清零
- **摘帽收尾**：类型接完后去掉 webview/main.ts 首行 `@ts-nocheck`，在 `strict:false` 下把
  tsc 报错清零（代码本就能跑，预计量不大；个别真类型问题顺手修掉）。从此 webview 类型门禁对
  main.ts 有真牙
- **价值**：toolEnd 丢 detail 那类 bug（宿主漏发字段，webview 静默拿到 undefined）从此是编译错误
- **验收标准**：`npm run compile` 全绿；`tsc -p webview --noEmit` 在**无 @ts-nocheck** 下零报错；
  grep 确认 post / onWebviewMessage / onPiEvent 链路无 `any`；
  行为零变化（纯类型层改造，不改任何运行时逻辑）

### 工单二：piClient 加固 + 工单二b：残留收口 —— ✅ 已全部完成（eb81981 部分 + 59c473a 收口，勿重复执行）

> 工单二原为「⚠️ 部分完成已验收（3/5 项，残留转工单二b）」，收口单二b 补完后合并归档。

对 [src/piClient.ts](src/piClient.ts)（eb81981 完成部分）：
- `send()` 加响应超时：超时 reject 并从 pending 清除（防 pi 活着但卡死时 refreshState 永久挂起）。
  超时值建议 30s（prompt 命令响应可能慢，注意区分「命令响应」与「agent 执行」）
- `stdin.write` 补 error 监听（防 pi 退出瞬间写 stdin 触发未处理 EPIPE 异常）
- `piVersion()` 加超时（防 `pi --version` 挂起泄漏子进程）
- **RPC 响应收口（工单一残留）**：getMessages/getState 等命令响应建型
  （复用 protocol.ts 的 SessionMessage 等），灭掉 [panel.ts:1744-1776](src/panel.ts#L1744) 三处 `(m: any)`
- 验收：`npm run compile` 全绿；grep 无 `(m: any)` 残留于消息链路

工单二b（59c473a 收口，开工前总监重读提醒已随单完成）：
1. **sessionList 死链删除**（裁决见「pi 请示裁决」第 3 条）：panel.ts:553 `case "listSessions"` +
   `sendSessionList()`（panel.ts:~906）+ protocol.ts:61/:296 两个类型，整链删除；protocol 留一行注释
   「面板内历史列表若重做再按需重建」
2. **RPC 响应收口**：piClient 的 getMessages/getState 等命令响应建型（复用 protocol.ts
   的 SessionMessage 等），灭掉 [panel.ts](src/panel.ts) 4 处 `(m: any)`
3. **toolDetail 用例入库**：工单三声称的「7 组口径用例」以文件形式进仓库
   （`src/toolDetail.test.ts`，脚本在 scripts/ 下可跑），用例不再口头汇报
- 验收：`npm run compile` 全绿；grep 确认 sessionList/listSessions 全仓零残留、
  消息链路无 `(m: any)`、用例文件存在且可跑

### 工单三：统一 toolDetail 双实现 —— ✅ 已完成已验收（4842557，勿重复执行；用例补交归工单二b）

AGENTS.md 债务清单第 2 条：宿主 [panel.ts](src/panel.ts) `toolDetail` 与 webview [main.ts](webview/main.ts)
`historyDetail` 已漂移（name vs file/cmd 字段、后者多首字符串兜底）——同一工具调用流式时与历史重绘
显示的摘要可以不一样。

- 抽成共享实现（放两端都能 import 的位置，随 protocol.ts 一起落地），字段取并集
  （command/file_path/path/url/query/pattern/content/skill/name/file/cmd + 首字符串兜底）
- **本单是唯一允许的行为变更**：live 与历史渲染从「各说各话」收敛为一致，此为修复目的本身
- 验收：`npm run compile` 全绿；同一会话流式中与 settled 重绘后的工具摘要一致

### 工单四：panel.ts 核心/宿主分层——接线专项 ✅ 已完成已验收（b981fe1 + e04a1dd + 00c13e2，勿重复执行）

**现状**：src/piCore.ts（1005 行）与 src/hostCapabilities.ts 已存在（另一会话半成品，
已被 release 扫进仓库），panel.ts 未 import，零行为影响。总监已核：头注释搬运纪律明确、
UiActions + HostCapabilities 双接口设计符合本工单、grep 零 vscode 实 import。
**本单任务 = 接线收尾，不是重写**：

1. **对账移植**：piCore.ts 方法体自旧版 panel.ts 逐字符移植——但 panel.ts 此后前进了
   （原生斜杠命令表 ce8521b、/compact 直压 810f39c、删除入口 4781f78 等均不在 piCore 里）。
   逐方法 diff 旧版（git show 55e977b:src/panel.ts 前后基线自选，写清对账基线）与现 panel.ts，
   把漂移补进 piCore 或确认归属 adapter，**不得凭记忆搬运**
2. **接线**：panel.ts 收缩为 VS Code adapter——实现 HostCapabilities + UiActions，
   组装 piCore 核心控制器；extension.ts 改挂新组装入口（若需要）
3. **死链顺手删**（总监裁决，随本单）：panel.ts:607 `case "deleteSession"`（裸 rmSync、
   webview 零发送端）+ protocol.ts:73 类型整链删除，protocol 留一行注释
   「删除走宿主 QuickPick deleteSessionPick 路径（4781f78），webview 直删不再提供」
4. **AGENTS.md 更正**：「piCore/hostCapabilities 保持未跟踪」表述已失效（已入库），
   按接线后事实改写
- 验收：`npm run compile` 全绿；piCore.ts grep 零 `vscode` import（注释除外）；
  行为零变化（移植对账表可查）；死链删除后 grep 全仓零 deleteSession 残留
  （deleteSessionPick 宿主本地函数除外）；webview 与核心间消息类型全部来自 protocol.ts
- **边界（不许顺手改）**：不改 webview/main.ts 与 protocol 既有消息语义；
  不同步做状态机收敛/IO 异步化（后续排队各自立项）；产品线功能（压缩横幅等）
  等接线落地后另签，插在 HostCapabilities 上

### 工单五：RPC 遗产清理——直连后的状态机简化 —— ✅ 已完成已验收（勿重复执行）

直连后 `session.isStreaming` / `pendingMessageCount` 是同步真读，RPC 时代为「猜状态」
建的多层防御已失去存在理由。逐项处理，**每项独立提交**：

1. **三层对账通道简化**：事件流对账（onPiEvent 顶部）+ get_state.isStreaming 对账
   （refreshState）在直连下是永真保险——永真代码会掩盖新 bug。先在 dbg 日志加断言
   （isStreaming 与 busy 镜像不一致时记日志），观察一段时间确认零触发后删
2. **pendingPrompt 4s 兑底语义重审**：preflight 验收即回后，「命令式应答无 agent_start」
   仍存在（b040fb2 已把 /mode 类拦截在本地）；评估残留场景：还有哪些 prompt 不带
   本地拦截且不触发 agent_start；能删则删，不能删写明为什么
3. **steer 自愈可达性评估**：直连下 prompt 的 steer 参数由 wasBusy 镜像决定；镜像与
   pi 真相一致时 SDK 不会拒收。若自愈路径已不可达，标记死代码并删（拒收报错文案
   契约注释保留）；若可达，写清什么场景可达
4. **ship 脚本加固**：`git push && vsce publish` 失败静默（09-09 实际翻车一次：代理
   未开，push 失败连带 publish 未执行，终端不显眼）——失败必须红字提示并非零退出
- 验收：`npm run compile` 全绿；每项提交信息写清「删了什么、为什么现在能删」；
  UI 行为零变化（删除项以「不可能再触发」为标准，拿不准的不删，写明挂起）

### 边界（工单五不许顺手改，随工单五归档）

- 工单四 piCore 接线（另立会话专项做，搬运纪律见 AGENTS.md）
- panel.ts 其余状态标志的合并收敛（等工单五落地后重估哪些标志已无意义）

### 边界（工单一~五 本批工单不许顺手改，随批归档）

- panel.ts 的状态标志收敛（busy/pendingPrompt/abortSkipRender 等 18 个）
- 同步 IO 异步化（listSessions / applyHtml / dbg）
- 多标签并行会话（架构级，单独立项）
- 除工单三外任何行为变更——本批其余只做类型与防御，UI/交互不动

### 工单六：压缩事件显性化 + 上下文阈值预警 ✅ 代码层已验收（4d89841，勿重复执行；实测尾巴见 DIRECTOR 未结事项）

工单四已落地，核心能力全部走 caps/UiActions，产品功能开抢单。本单命中用户真实痛点
（glm-5.3-flash 上下文 >30% 变不准）：**压缩发生用户不知情、该压了没人提醒**。

1. **auto compaction 显性化**：pi 自动压缩发生时（piClient/piCore 事件流可辨），面板顶部
   横幅「上下文已自动压缩」+ 时间戳，用户可手动关掉；不打断对话流
2. **contextPercent 阈值预警**：上下文占用超过阈值（默认 70%，VS Code 设置可调）时，
   在横幅位提示「上下文已用 N%，建议压缩」，附一键 compact 按钮（直压语义，同 810f39c 口径）；
   同一会话只提醒一次，压缩后或新会话重置
3. **协议同步**：新增跨边界消息三处同笔（发送方/接收方/protocol.ts），
   未验证的 pi 字段一律保守可选（裁决 4）
4. **架构纪律**：横幅状态机放 piCore（不碰 vscode），宿主 UI 用现有 post/notifier 链路；
   阈值读取走 caps.getConfig
- 验收：`npm run compile` 全绿；构建 vsix 实测（自动压缩横幅 + 阈值提醒 + 一键压缩
  三场景）；webview/main.ts 保持无 @ts-nocheck 零报错；协议消息三处同步
- **边界（不许顺手改）**：不动状态机既有标志语义；不做手动 compact 入口改造
  （现有 ⚡/compact 已够用）；不做 contextPercent 的进度条可视化（等实测反馈再议）；
  piCore 若发现接线遗漏的漂移，先报告再动工，不混进本单

### 工单七：pi 变更 Git diff 可视化 ✅ 代码层已验收（191231f + 603b58c，勿重复执行；实测尾巴见 DIRECTOR 未结事项）

**用户拍板 A 原案（09-09，三选项 A/B/C 中选定）**：完整方案，含 SDK 查证与快照比对候选。
产品线第 5 项提前提级：**终端做不到的操作，Auto 模式用户的安全网**——pi 干完活，
改动以 VS Code 原生 diff 呈现，单文件一键还原（git 为 baseline）。
**多标签预留（用户拍板）**：核心侧产出的变更清单按会话域持有（每个 PiCore 实例一份），
多标签并行会话落地时直接复用为归属层。

**第 0 步（强制，先报告再动工）**：查证 pi SDK 侧文件变更感知途径——
① 事件流/命令响应里有无文件变更信息（file change 类事件、getCommands 之外的扩展命令）；
② 若无，方案 B：agent_settled 时对工作区做 git status/diff 快照比对（run 开始前快照）。
把查证结论与选型写进 BUILDER.md 请示，总监裁决后再施工——本单不做盲选。

选定方案后实施（**选型已裁决，见裁决 11：混合方案批准**）：
1. **核心侧（piCore）**：复刻 pi compaction/utils 的工具调用提取逻辑（函数未导出，
   ~20 行；注释留出处与 pi 版本号，升级 pi 时对账；写类工具清单以 pi 工具 schema
   为准枚举，不硬编码猜），产出「本轮变更文件清单」中性事件（经 HostToWebview 或
   直接宿主回调），不碰 git/vscode 语义
2. **adapter 侧（panel.ts）**：git 取 baseline（agent_start 时 git status 快照 +
   settled 时 diff 比对兜底 bash 改动）、调 VS Code 原生 diff 视图、单文件一键还原
   （还原必须二次确认，破坏性操作最高规格同 deleteSession；**还原范围安全边界见裁决 11**）
3. **入口**：agent_settled 后面板出「查看本次改动」入口（横幅或状态栏，不强制弹）
- 验收：`npm run compile` 全绿；构建 vsix 实测（pi 改多文件 → diff 列表 → 单文件
  还原 → 内容确实回滚）；piCore 零 git/vscode import；协议消息三处同笔
- **验收尾巴（实测过随 0.0.81 执行）**：README 中英「特性」节增补变更可视化条目，
  文案草稿（用户已过目，口径「差异化不称独一」）：
  中：**📊 变更可视化与定点还原** — pi 干完活，面板直接给出本轮改动清单，点开即看
  VS Code 原生 diff；不满意的文件一键还原。git 仓库内走基线回滚，仓库外靠工具级补丁
  逆向——混着你自己改动的工作区也不会误伤，只退 pi 碰过的。敢开 Auto 模式的底气。
  英：**📊 Change Review & Targeted Revert** — After each agent run, get a per-run
  change list with native VS Code diffs and one-click per-file revert. Git-baseline
  rollback inside repos, tool-patch-based revert outside — never touches your own
  uncommitted work. The confidence to run in Auto mode.
  配图：横幅+diff 同框截图；push 后市场页才不裂图
- **边界（不许顺手改）**：不动工单六横幅语义（入口可复用横幅位但不动其状态机）；
  不做整会话多 run 聚合 diff（单 run 粒度先落地）；不碰 piClient.ts（注释挂账另清）

### 工单八：行内审查 —— ❌ 判死（用户实测否决，2026-09-10，勿复洁）

用户两轮实测后判决：「这个 diff 非常鸡肋，我完全可以用源代码管理看，现在就保持简洁」。
**教训入账**：行内审查（高亮/评论框/逐块还原）与 VS Code 原生 SCM 能力重叠——
「看改动」原生面板已完美，插件硬造一套是重复造轮子；工单七的独有价值只在
**run 归因**（本次改动条 + 文件清单 + 一键直达原生 diff + 整文件还原），这条保留。
代码已全部退回 HEAD（panel/piCore/patchRevert/i18n/package 五文件），简洁版即工单七完整态。
**下次再想做行内审查前重读本条**：先回答「这比 SCM 多了什么」，答不出就不做。
**终审原则（用户 2026-09-10 拍板）**：「能用原装就用原装」「就保留查看按钮得了」——
入口唯一（变更条上的查看改动按钮，⚡ 备用入口已撤），点开后文件清单行内按钮
直达原生 diff / 整文件还原，审查渲染全用原装能力

用户提案（2026-09-10）：「有改动的话给改动区域加背景色，旁边加还原/保留按钮」——
Cursor/Copilot 同款行内审查体验，是工单七交互的自然收尾，比清单绕一圈直观。

**已实现（总监例外施工，随实测批次待追认）**：
- 命中文件在编辑器里改动行整行暖橙背景（isWholeLine 装饰器）
- 每个改动块一条 CodeLens：↺ 还原此块 / ✓ 保留修改（回车不干预）
- 还原此块：模态确认 + 脏锥冲拒绝 + 锦点校验（reverseApplyOneHunk 复用 patchRevert
  的校验纪律，锚点不符拒打不落盘）；文本拼接不依赖 git（新旧两侧行都在 diff 里）
- 保留修改：清除该块高亮（纯视觉确认，不动磁盘）
- hunk 来源：tracked+inHead 走 git diff -U0；未跟踪单 patch 走 parseHunks；
  多 patch 链未跟踪文件行号已漂移不拍平（整文件还原走清单，不误导）
- 生命周期：run 作用域（settled 重建/整文件还原清/换会话清/宿主重载清）
- **红绿区分（用户反馈融合常规 diff 观感，2026-09-10 二轮）**：绿底 = 新增行
  （diffEditor.insertedTextBackground 主题自适应，弃土黄）；红 = 被删行以幽灵行
  红字标在删除点（before 伪行「⤷ 已删除: …」，超长截断——被删行在当前文件
  不存在，行内模式的红色只能以幽灵形式落）；悬停橙区出操作卡（大号 command
  链接，CodeLens 颜色主题定死灰色不够醒目的补救）；patchRevert 增 removedLines
  与单 hunk 逆向导出
- patchRevert.ts 新增导出 parseHunks/reverseApplyOneHunk（复用内部 parsePatch 与校验）
- **二轮重构（用户两连拒：CodeLens 灰字丑、红幽灵行锚点漂移）**：行内装饰只留
  淡绿底+左色条；审查动作改 GitHub PR 式评论框（CommentController 线程，
  正文 ```diff 块红绿原生渲染 + comments/comment/title 真按钮）；CodeLens/
  悬停卡/幽灵行全部退役；patchRevert 增 diffText 与 removedLines 导出
- 验收：compile 全绿 + test:revert 12 断言过 + 用户装包实测（高亮出现/逐块还原
  内容回退/保留清高亮/锚点漂移时拒打）
- **结局（2026-09-10 用户终审）**：行内审查整体判死，代码退回 HEAD；但其驱动
  的两轮实测抓出三处真问题并已修复入账——toolCallPaths（patch 归档从未工作过）、
  未跟踪文件无对比（补「本轮改动前 ↔ 当前」路径，adc353d）、变更条文案歧义
  （89a2f35）；README 双语增补同步落地（工单七验收尾巴完结）
- **边界（不许顺手改）**：git-only 文件不高亮（归属不明）；不做跨会话持久化；
  多 patch 链不做逐块（行号漂移，误导性大于收益）

### 工单十：dbg 日志上限轮转 + importSession 大小上限 —— ✅ 已完成已验收（10ef229 + 7158af5，总监验收 8.5/10，勿重复执行）

> ⚠️ 事故附记：施工方（小模型）曾自行把本单标为「已完成已验收」——**验收是总监的
> 动作，施工方自盖章无效**。本次恰逢总监抽验通过（纪律面全过：守「勿加仪式性代码」
> 边界、双语同笔、独立拆笔、compile 复跑绿），结论碰巧为真，但权限非法；若自盖的
> 章内容有假，整个「勿信 commit message、以本文件为准」的台账信任链就断了。
> 遗留小刀：importSession 大小检查位置在 ensureClient(true) 之后（panel.ts:839），
> 拒收大文件仍会无谓重启客户端，应上移到 .jsonl 检查旁。下位施工者顺手修

审计两条成立项合并为一单（同为运维防御小刀）：

1. **dbg() 日志上限/轮转**（piCore.ts:137）：`fs.appendFileSync` 无限增长属实，
   且含会话内容。加大小阈值（5MB）超限轮转（rename 旧文件留一代再 append，
   旧文件最多一代、再超限覆盖——诊断日志可重放，不需持久保留）
2. **importSession 大小上限**（panel.ts:823）：copyFileSync 前查源文件大小，
   超上限（50MB）拒绝并转 notice。注意：审计报的路径遍历**不成立**
   （path.basename 已剥目录成分，无分隔符无从穿越）、内容校验**不成立**
   （非法 JSONL 在 switchSession/getMessages 抛错已有 catch 落 notice），
   此两项不做，勿顺手加仪式性代码
- 验收：`npm run compile` 全绿；dbg 超限后日志文件不超阈值；导入超大 jsonl 被
  拒且有用户可见提示
- **边界（不许顺手改）**：dbg 的调用点与日志内容格式不动；importSession 的
  copyFileSync 流程结构不动（只加前置检查）

### 工单十一：工具命中路径锚定工作区根 —— ✅ 已完成已验收（43640c6，实测 2026-09-11 通过，勿重复执行）

**现象**：pi 改动 gitignore 文件（DIRECTOR.md）后，变更条「查看改动」→ diff 报
「由于找不到该文件，因此无法打开编辑器」。

**根因（总监已实证，勿重查）**：pi 的 edit args.path 原样保留模型传入的相对路径
（本会话事件流实查：`"path":"DIRECTOR.md"`）。相对路径一路进归因清单 →
`openPrerunDiff()` → `vscode.Uri.file("DIRECTOR.md")` 不基于工作区根解析，
落成盘符根 `/DIRECTOR.md` → 文件不存在。**与 .gitignore 无关**：被忽略只是走了
prerun 路由的巧合；tracked 文件 + 相对路径同样会死（Uri.file/`fs.readFileSync`
拿相对路径全链路歪）。之前实测未炸纯属当时模型传了绝对路径。

**第二症状（2026-09-10 晚，工单十二施工会话实证）**：tracked 的 package.json 的
diff 标题错走「本轮改动前 ↔ 工作区」（应为 HEAD ↔ 工作区）——ext host cwd ≠
工作区根时 `relOf` 的 `path.relative(root, 相对路径)` 得到垃圾值 → nowStatus 查不到
→ inHead 误判 false → 路由错进 prerun 通道（本次碰巧 patch 数据在，diff 内容侥幸
显示正确）。同根因，随本单一并修并复验。

1. **归因入口锚定**：panel.ts `handleRunSettled()` 对每个工具命中 `f.path` 先
   `path.isAbsolute` 检查，非绝对则 `path.join(root, f.path)`（root 已在手），
   归一化后入 changesDetail/changesFiles——单点修复，下游 diff/还原全链路收直
2. **顺带核查 headContent 的 slice 疑点**：`prerun%3A` 分支（9 字符前缀）用
   `slice(7)` 截会切出半编码串，decode 后带前导冒号 → changesDetail 查不到 →
   左侧白屏。改成按实际匹配前缀的长度截（或统一先 decodeURIComponent 整个
   query 再 startsWith("prerun:")），并把「uri.query 返回编码还是解码形态」的
   实证结论写进注释，两种 startsWith 分支之谜就此了结
- 验收：`npm run compile` 全绿；让 pi 用**相对路径** edit 一个未跟踪/忽略文件，
  变更条 diff 能打开且左侧「改动前」内容非空；还原该文件内容回退
- **边界（不许顺手改）**：piCore 归因结构不动（核心层不知道 root，锚定属
  adapter 职责）；git 兕底路径（已绝对）不动；不动裁决 11 还原边界
- **状态**：已施工，**代码层验收已过 9/10**（归档.md 验收记录 09-11 行：锚定/slice/
  症状二逐一实证，compile 全绿），**待用户装包实测**——0.0.87 包已重打含修复
  （旧包 09-10 20:39 打的不含，勿用旧包实测），实测过了本单全结


**实测记录（2026-09-11，用户 0.0.87 真环境）**：场景 B tracked 相对路径——diff 标题「package.json (HEAD ↔ 工作区)」正确（症状二修复实证）；场景 C 绝对路径回归正常；场景 A 复测（UTF-8 未跟踪文件 + edit 相对路径）——变更条出现、diff 打开、「改动前」非空、还原回退，三条全过，本单全结。

**实测插曲（留档）**：两轮「失败」均系测试文件为 PowerShell echo 写出的 UTF-16LE——bash printf 兜底不触发 edit 工具（无变更条属裁决 11 设计边界）；patch 逆向守卫在 \x00 夹心内容上失配拒显拒还原（守卫正确工作，磁盘文件实证无损）。结论：插件链路无恙，测试姿势问题。

**遗留小刀（不挡验收，随 0.0.88）**：未跟踪文件的 diff 失败占位文案「无法读取 HEAD 版本」不准确（未跟踪文件无 HEAD），应改「无法读取改动前版本」（i18n chgHeadFail，prerun 分支复用同一键所致）。
### 工单十二：ship/publish 机械闸门 —— ✅ 全结（2026-09-11 用户实测终验通过）

> **终验（2026-09-11）**：0.0.87 真发版全链路（hunk 有货 → commit → push → publish）
> + PI_CONFIRM_SHIP=1 快车道，用户实测通过——用户发话即为验收依据，不重复查证。
> 本单全结，全文迁回归档.md 存档；拒绝双路径此前已验（用户真终端 + 总监 EXIT=1 复测）。

> 原 DIRECTOR.md 中本单出现两次标题（总监编号重复写，实为同一工单）：
> 下块「事故与裁决」是起因，「全结」是结果。归档时完整保留两段。
> 【2026-09-11 改判史二则】①原标「✅ 全结」被用户纠正（全链路终验未走，定级失真），
> 曾回迁待终验；②同日用户实测终验通过，本单真全结。两次改判均留痕，勿删。

**事故（2026-09-10 擅自发版事故沉淀）**：新接入的小模型在未获用户发话的情况下跑了
完整发版一条龙（0.0.82，commit 4987898 已推 origin/main）。它读过的正是
AGENTS.md/DIRECTOR.md——「读而不守」比「不读」更危险：不读的会被 review 拦住，
读而不守的会拿文档当行动清单绕过怀疑直接干。
**且「git add -A 扫 WIP」第三次肇事**（工单四 piCore→test*.txt→本次附件路径功能被混进
release 提交）。

**裁决：把「push/publish 必须用户明确发话」从口头约定升级成机械闸门**——读而不守的
模型防不住，闸门比人品可靠：

1. ship/publish 脚本（scripts/publish.js 或 ship 入口）开头加交互确认：显示目标版本号
   与将提交的文件清单，需键入 `yes` 才继续；非 TTY 环境要求 `PI_CONFIRM_SHIP=1`
   环境变量，二者缺一即红字退出非零
2. `git add` 改为显式文件列表或至少 `git add -A` 后、commit 前 diff 逐 hunk 打印归属确认
   （AGENTS.md「同一工作区 git 单写方」约定的机械化）
- 验收：非交互环境无确认变量跑 ship → 红字拒绝非零退出；带确认跑通全链路
- **边界**：不改 release/publish 本身逻辑，只加前置闸

**用户真终端实测（node scripts/publish.js，不跳版本号）双路径通过**：
①首确认键 no → 红字取消零副作用 ✅；②键 yes → add → hunk 环节因工作区干净走
「暂存区无变更」警告跳过 → commit「nothing to commit」红字非零退出 ✅（无物可提交，
止血链正确）。交互路径（readInput）在 no/yes 两向均已人肉验证。

**挂账一条**：hunk 有货 → commit → push → publish 全程只在真发版时走到，
**0.0.87 发版即全流程终验**，届时顺带验证 PI_CONFIRM_SHIP=1 快车道全流程。

**改判史（留档，勿删）**：一轮实现（a88fb9a）用 `while(true){process.stdin.read(1)}`
紧循环读输入——阻塞事件循环后 I/O 永不处理，read() 恒 null，总监管道实验 exit=124
实证必挂死，TTY 同机制同杀，所有路径必死；施工方「PI_CONFIRM_SHIP=1 → 正常运行 ✅」
为假账。教训两条：①关键路径零验证时默认按坏处理，让人证伪而非证实；②汇报的
验证必须与实际路径一致。用户质疑「你确定验收没问题」触发复检——质疑有效，
首轮「有条件通过」定级作废。

**保留的验收通过项（a88fb9a + 76dbb0e + 5da30df + 5208894，不重做）**：pre-add 必挂检查已消、
未推红线守住、闸门四要素流程编排与事故头注释、DIRECTOR.md 只读锁被挡后自行转请示的行为。
**已知设计取舍（记录不改）**：① PI_CONFIRM_SHIP=1 可被模型自行设置绕过——闸门
防误触发不防蓄意，蓄意绕过会留命令痕迹可 review；② 闸门在 npm version
bump/package 之后——拒绝后本地版本号已 bump，号位跳号可接受

## 二、验收记录（review 历史，原 DIRECTOR 第四节）

| 日期 | commit | 结论 |
|---|---|---|
| 09-09 | 477f8a1 | webview 工程化：8/10，搬运纪律正确，双逗号/setStatus 重复随之消灭 |
| 09-09 | 9e46343 | toolEnd 蒸发修复+死代码清理：9/10，修复链完整，遗留 4 项小问题 |
| 09-09 | e87a411 | 按 review 收尾四项：满分，门禁实测全绿，无假账 |
| 09-09 | f278f2f | 工单一 protocol.ts 建型 + 摘 @ts-nocheck：9.5/10，三链路 any 清零、门禁有真牙；残留 getMessages 响应 any 归工单二 |
| 09-09 | eb81981 | 工单二 piClient 加固：**部分验收 6.5/10**——超时/EPIPE/piVersion 三项扎实（含 rpc.md 前提核实，好活）；**漏做 2 项**（sessionList 清链、getMessages 建型），系未重读 DIRECTOR.md，转工单二b |
| 09-09 | 4842557 | 工单三 toolDetail 统一：**验收通过 9/10**——并集 11 键实现干净、行为变更两处均属工单允许范围；扣分：「7 组用例」未入库不可复验，补交归工单二b |
| 09-09 | 59c473a | 工单二b：验收通过——三项全完（sessionList 清链零残留、4 处 (m:any) 清零、27 断言用例入库可跑）；StateMsg 顺带修追认 |
| 09-09 | 55e977b | **直连架构（用户直令）：验收通过 9/10**——端到端+包级双验证，拒收文案/时序契约保真；扣分：AGENTS「WIP 不入库」被 release 扫库打破、ship 静默失败险情 |
| 09-09 | 2e773b3 + fc134df + b040fb2 | 直连后修复三连：抽验通过，compile 全绿，语义与直连一致；计时重置修的是 adcaaef 漏挂乐观置位，徽标修的是兑底空串，/mode 修的是扩展命令假忙 |
| 09-09 | 366a701 | 发布 0.0.78：push + 市场落地确认（「already exists」反向证实）；ship &&-链静默失败教训入工单五 |
| 09-09 | d22c3a3 + ce8521b + 78287ac + 6eb8244 | **工单五：验收通过**——四项全落，①观察断言零行为变化；②③保留结论注释到位（②实体被 ce8521b 并库，归属已知悉）；④publish.js 红字加固。编译门禁全绿。注：验证脚本时误推 3 提交上 origin，用户已知悉；角色分工后本会话不再执行施工 |
| 09-09 | 810f39c | /compact 直接压缩：**追认 9/10**——withNote 参数化干净，⚡ 菜单保留带指令入口（用户主动选不算打扰），口径自洽；compile 全绿 |
| 09-09 | 4781f78 | 会话删除入口：**追认 8.5/10**——守卫实证过关（lastSessionFile+samePath 挡当前会话、realpath+startsWith(root)+path.sep 挡路径穿越、模态确认、i18n 双语+NATIVE_KEYS 注册齐全）；扣分：死链 case "deleteSession"（607 裸 rmSync）+ protocol.ts:73 类型未删——有了安全路径后无守卫死链更碍眼，裁决随工单四删整链。技术债 #4 销案 |
| 09-09 | 632ffec + 553f97f + f04c40d + 6d9f342 | 文档收口与两轮 release：抽验通过（FEATURES 引用零残留、0.0.80 已发布）；文档漂移由本次验收循环对账消灭 |
| 09-09 | b981fe1 + e04a1dd + 00c13e2 | **工单四接线：验收通过 9.5/10**——对账基线可证（55e977b）、漂移逐 commit 归属（含 2e773b3/fc134df/d22c3a3 易漏项）、死链三处同步删、跨模块契约（steer 正则/4s 兕底/elapsedMs/newLive）逐一实证在位；panel 2252→1338 行、webview/package.json 零改动。扣分：piClient.ts:20 注释指向未跟搬（挂账）；compile 全绿 |
| 09-09 | 5aca0f0 | 裁决 9 沉淀 AGENTS.md：通过，台账转 ✅活 |
| 09-09 | 4d89841 | **工单六：代码层验收通过 9/10**——协议三处同笔、横幅状态机 piCore 不碰 vscode（setBanner 变更才下发 + webviewReady 握手重发）、result 保守 unknown 守裁决 4、阈值经 caps.getConfig 默认 70、webview ES5 var 只渲染不持状态、压缩失败透传 notice（好细节）、边界全守（无状态机语义变更/无进度条/手动压缩未动）。vsix 三场景实测挂账待用户，实测过全结 |
| 09-09 | 603b58c | AGENTS.md 沉淀还原边界：通过——「别优化掉任何一道守卫」写进不可破坏约定，patchRevert 入结构表 |
| 09-11 | 43640c6 | **工单十一：代码层验收通过 9/10**——单点锚定实证（handleRunSettled 对 f.path isAbsolute→join(root)→归一化入 changesDetail/changesFiles，下游 diff/还原全收直）；headContent slice 疑点同修：统一 decodeURIComponent 后 startsWith("prerun:") 按前缀长度截，实证结论入注释；症状二（relOf 垃圾值→inHead 误判）同根因一并修，边界全守（commit 仅 panel.ts，piCore/git 兜底/裁决 11 零触碰）；compile 全绿。注：旧 0.0.87 包（09-10 20:39 打）不含本修复，已重打新包；用户实测挂账待过 |
| 09-11 | a888e3c + 94ff28d + 573f0c4 + 9a2f4b6 + d1d281c | 文档五连抽验通过：逐笔 git show --stat 无混笔（文档/清理各自独立）；test.txt/test111.txt 已清（0.0.87 攒包项销账）；内部文档入 git 系 94ff28d 用户拍板，.vscodeignore 排除在位（vsix 泄漏红线未破，已实测新包不含内部文档）；总监.md「内部文档不进 git」旧口径随之作废并修正（git 入库、vsix 仍排除） |
| 09-11 | 全链路终验（发版动作，无独立 commit） | **工单十二终验通过，全结**——0.0.87 真发版走完 hunk→commit→push→publish + PI_CONFIRM_SHIP=1 快车道，用户实测发话即为验收依据；拒绝双路径此前已验。两次改判（「全结」→回迁待终验→真全结）均留痕于工单头部 |
| 09-09 | 191231f | **工单七：代码层验收通过 9.5/10**——裁决 11 四条边界逐一实证：还原资格函数只放行 tool 命中（git-only 纯展示）、revertable 仅 edit+patch（write 不可还原）、patch 链逆序逆向任一步不符拒打不落盘、模态确认带 preexisting+脏缓冲警告（好细节）；git 兑底排除运行前已 dirty（归因噪声控制）；piCore 中性回调零 git/vscode、args 保守检查守裁决 4；清单会话域持有（多标签预留落地）；patchRevert 12 断言入库可跑（test:revert）；协议三处同笔；compile 全绿。vsix 实测挂账待用户 |

## 三、安全审计裁决记录（2026-09-10，只留档不施工）

外部审计 12 条，总监逐条核实后裁决。**签发**：#1→工单九（降级 P2）、#4→工单十（P1）、
#6 大小上限部分→工单十。**拒单（不成立或非本层职责，勿再翻案施工）**：

- **#1 XSS 定性驳回**：审计没核 CSP nonce 与两条 URL 分支白名单就报高危，攻击路径
  逐跳验证均走不通（详见工单九头注释）
- **#2 API Key 明文**：`~/.pi/agent/auth.json` 是 pi 本体存储格式（与 pi CLI /login
  一致），本层单方面加密 = 破坏与 pi 核心兼容。**上游债务**：要改去 pi 仓库提
- **#3 npm install 沙箱**：供应链投毒时任何安装入口同样中招，本层做不出有意义的
  预检，纯仪式性代码。不做
- **#5 原子写**：temp+rename 在 Windows 有 rename-over-existing 语义坑，且断电窗口
  概率与代价均低。**记债务不排期**，哪天动 revertFile/configApiKey 顺手改
- **#6 主体**：路径遍历与内容校验两项不成立（证据见工单十-2）
- **#7 竞态**：onRunSettled 回调传快照副本（piCore.ts:1207），异步 gitStatus 期间
  map 被改不影响本次归因；diff 归因固有近似只影响展示，不碰破坏性操作。不修
- **#8 Webview 消息伪造**：webview 消息只能来自 webview 内部，VS Code 标准信任模型；
  开发者模式可篡改时攻击者已能改一切本地代码。接受现状
- **#9-12 低危**：同意审计原判，不动

## 四、历史状态沉淀（自原 DIRECTOR「一、当前状态」迁入，已完成部分）

- **架构已换代（55e977b，用户拍板）**：piClient 从 spawn `pi --mode rpc` 子进程换为
  进程内 AgentSession 直连（新增 src/piSdk.ts 定位并加载用户已装的 pi 包）。RPC 实现
  整树保留在分支 `rpc-subprocess`。动机与语义契约见 piClient.ts 头注释「RPC 语义保留对照」
- **0.0.80 已发布市场**（f04c40d→6d9f342 两轮 release；本地与 origin/main 同步）。
  期间 FEATURES.md 已退役（553f97f，功能展示以 README 为唯一门面）、package.json 描述
  去掉过时 RPC mode 标注（4781f78）
- **工单四接线已验收通过（00c13e2，三连 b981fe1→e04a1dd→00c13e2）**：panel.ts
  2252→1338 行收缩为纯 VS Code adapter，piCore（1088 行）正式上岗。对账基线明确
  （55e977b，以 e562731~1..55e977b 为空证明移植基线），漂移清单逐 commit 归属补齐
  （斜杠命令表/直压/计时修复/徽标回退/观察断言）；死链 deleteSession 三处同步删；
  steer 自愈正则、4s 兜底、elapsedMs、newLive 全部实证在位；webview/package.json 零改动
- **裁决 9 已落地（5aca0f0）**：git 单写方约定沉淀进 AGENTS.md，台账已转 ✅活
- **打包事故双入账（2026-09-09 深夜，用户装包实测无法激活时暴露）**：
  ① **f2b42a3 的 .vscodeignore 排除（out/piCore.js + hostCapabilities.js）在工单四接线后
  未回收**——接线后打的每个 vsix 都是必挂包（panel require ./piCore 而包内无文件），
  20:50 的本地包即此；市场已发布的 0.0.80 是接线前构建，反而无害。教训：排除规则
  是临时手段，入包/出包必须随接线状态同步回收；② **内部文档泄漏**：AGENTS/DIRECTOR/
  BUILDER/总监.md 无排除规则，随历次 vsix 发布到市场（0.0.78 起已装目录实证）。
  总监例外施工修 .vscodeignore（删两行排除 + 加四文档排除），重打包验证：
  piCore/hostCapabilities 在包、内部文档零残留。已发布历史版本无法撤回，0.0.81 起干净。
- **待追认批次（5f30a51，用户指令执行）**：①piCore 补 compaction_start no-op case（假警告，
  工单六遗留——start 事件只入协议未消费落 default 透传）；②panel ⚡菜单加「查看本次改动」
  入口（变更条 ✕ 收起后查看能力不丢）；③compactSession 补 try/catch——pi 对过小会话
  直接抛 "Nothing to compact"，此前零反馈，现转译为「上下文占比很小，无需压缩」/
  「已压缩过」/失败原因，i18n 双语同步；④piCore.onWebviewMessage 补类级兜底 catch
  （原 switch 无外层接错，async 抛错静默蒸发——③零反馈的机制级根源），漏网异常统一转
  opFail 通知；⑤showChangesFlow 交互拍扁（两层 QuickPick 不友好）：文件清单行内按钮
  直达 diff/还原，回车默认开 diff，还原后原地刷新清单；⑥（dogfood 实测新抓真 bug）
  patch 归档从未工作过：tool_execution_end 事件不带 args（实查 pi-agent-core
  types.d.ts:410），原门控 e.args.path 永远失败 → patches 恒空 → 未跟踪 edit 文件永远
  「不可还原」。改用 start 时记 toolCallId→path 映射（toolCallPaths），end 按 toolCallId
  归档 patch，run 边界同步清理。README 的 pi 测试残留与 test*.txt 产物另随 0.0.81
  README 增补一并处理
- **0.0.81/0.0.82 已发**（2026-09-10 两次更正）：挂账 7 笔（含必挂包修复 5f30a51）已随
  0.0.82 实际上线，市场版现在是可用态；0.0.83-0.0.86 被工单十二真终端测试消耗
  （仅本地 bump 未发布，5208894 已随 fix 入库）；**下一版从 0.0.87 起**
- **工单四 WIP 曾被 ship 的 `git add -A` 扫进仓库**（piCore.ts 1003 行 +
  hostCapabilities.ts，随 e562731 入库）：仍未接线，panel.ts 不 import 它们；接完前
  门禁若被其类型错误拦住就地修类型不修行为（已发生一例：getConfig 泛型字面量收窄）
- **AGENTS.md 约定变更**：pi 包进 devDependencies（类型参考，不进 vsix）；
  「零运行时依赖」铁律保持（dependencies 仍为空）
- **用户实测结论**：流式与终端同速；插队五连实战全绿（queue_update 转正无一误判）；
  27s/52s「无输出」轮经 dbg 日志 + 充值背景定性为免费档配额龟速，非插件问题
- **直连后修复三连**：2e773b3 插话计时重置、fc134df 徽标清空、b040fb2 面板 /mode 假忙
  4s（SDK 扩展命令立即执行无 agent 事件，乐观 busy 被兑底误清）
- **事故记录 + 版本对账（2026-09-10，小模型擅自发版）**：
  - **4987898（0.0.82）混入的附件路径功能归属已正名**：主会话正当 WIP（用户直令产物），
    非小模型工作——小模型只是跑 release 时被 `git add -A` 连带扫入。监理初查 diff：
    结构守约（协议三处同笔/保守检查/注释齐，动机合理：绕开 200KB 内联上限让 pi 自己读）。
    **待补验收尾巴**：用户实测拖拽附件路径模式（pi 能读到文件 + 5 个/200KB 限制语义确认）
  - **版本对账更正**：0.0.81（5d9bb68，含 test.txt/test111.txt 垃圾被扫入，下版 git rm +
    核查 .vscodeignore）、0.0.82 均已发布上线；挂账 7 笔未发版已随 0.0.82 实际上线
    （含必挂包修复 5f30a51——市场版现在是可用态）；**下一版从 0.0.83 起**
  - 待用户确认：0.0.81/0.0.82 两次发版是否用户授权；未授权部分按本工单头事故定性
  - 小模型用人裁决：施工位不合格（读而不守），只可干只读活（查代码/读 diff/写草稿），
    不碰 git 与发版链路；模糊指令（「跑一下验证」）会被它往大了解释，禁用（详见总监.md）

## 五、与 AGENTS.md 的交接查证（原 DIRECTOR 六节，2026-09-09）

pi 自写的 [AGENTS.md](AGENTS.md) 已 review，**三条关键事实声明逐一查证，全部属实**：

1. `webview/main.ts` 首行 `@ts-nocheck` —— 属实（[main.ts:1](webview/main.ts#L1)），
   由此修正了「类型门禁已有牙」的验收结论
2. prompt 被 pi 拒收（already processing）时自动转 steer 重发 —— 属实
   （[panel.ts:427-434](src/panel.ts#L427-L434)，含完整注释），「steering 自愈别删」成立
3. tasks.json 有 `watch all`（tsc + vite 双 watch）任务 —— 属实

**附注**：AGENTS.md 债务清单 ↔ 工单对齐：①protocol.ts → 工单一（含摘 @ts-nocheck）✓；
②toolDetail 漂移 → 工单三 ✓；③同步 IO / ④状态机 → 后续排队 ✓；
piClient 加固 → 工单二（手册补记即可）

## 六、pi 请示裁决（史条目存档；活条目保留在 DIRECTOR）

> ✅史 = 裁决已定且已执行完，只留存档。以下条目随对应工单完结转档；
> ✅活 条目（2/4/6/7/8/9/11）仍在 [DIRECTOR.md](DIRECTOR.md) 约束后续施工。

1. ✅史 摘帽实测 57 错（48 个同类 TS2554）：认可记录在案，总监「预计量不大」的估计偏小——
   教训「估时以实测为准」已生效，本条只留档
3. ✅史 sessionList 死链：grep 坐实后**删整条链**（工单二b 已执行完），protocol 已留注释
5. ✅史 StateMsg `?: string \| null`（59c473a）：按「宿主实际发什么」定契约，已落地
10. ✅史 deleteSession 激活方案（4781f78，用户直令）：二级选择+模态确认+双重守卫追认；
    死链 case 与 protocol 类型整链删除裁决见工单四-3

## 七、当前未结事项（账目，随发布/验收推进）

> 自原 DIRECTOR「一、当前未结事项」迁入，只存档账目事实，不发施工指令。

- **下一版 0.0.87**（package.json 已 0.0.86；0.0.83-0.0.86 被工单十二真终端测试消耗，仅本地 bump 未发布）
- **实测尾巴（代码层已验收/已施工，待用户装包实测）**：
  - 工单六：自动压缩横幅 / 阈值提醒 / 一键压缩三场景
  - 工单十一：相对路径 edit 未跟踪/忽略文件 → 变更条 diff 能打开且左侧「改动前」非空、
    还原内容回退；顺带复验工单七变更条（含 gitignore 场景）
  - 附件路径模式：用户实测拖拽附件路径（pi 能读到文件 + 5 个/200KB 限制语义确认，工单十二归档附记）
- **0.0.87 攒包**（搁置清单，随发版一次性带出）：test*.txt / test111.txt 垃圾 git rm + 核查
  .vscodeignore、i18n 死键（fileTooBig×2）、顺手小刀（工单十遗留：importSession 大小检查
  上移到 .jsonl 检查旁，避免拒收大文件仍无谓重启客户端）
- **待用户确认（历史遗留）**：0.0.81（5d9bb68）/0.0.82（4987898）两次发版是否授权；
  未获授权的部分按「工单十二事故」定性
- **0.0.87 发版即全流程终验**：ship 闸门 hunk→commit→push→publish 全程 + PI_CONFIRM_SHIP=1
  快车道 —— **✅ 已终验通过（2026-09-11 用户实测，工单十二全结）**

## 八、后续排队（未签发，勿提前施工）

> 自原 DIRECTOR「三、后续排队」迁入，未签发前不做，防提前施工。

**工程线**：
1. panel.ts 状态机收敛（删 4s pendingPrompt timer hack）
2. applyHtml 同步读大图（listSessions 链已由工单十三异步化）
3. deleteSession 加 sessions 目录校验（deleteSessionPick 已有 realpath+startsWith 守卫，
   本条待重估是否已成死项）

**产品线**（信任工程，全部在工单四分层完成的基础上，以集成模块形态插在 HostCapabilities 上）：
4. Problems 面板错误一键发 pi（错误文本+文件+行号打包进输入框，手动触发不做全自动）
5. 面板内多标签并行会话：**已降级为按需**（用户洞察：实际业务并行需求少，见 09-09 DS 对话复盘）；
   HANDOVER.md 有设计草案，架构级，需专项会话

## 九、施工回报存档（BUILDER.md 迁入，2026-09-11）

> 自 BUILDER.md 迁入的**已完结工单施工回报与历史工作记录**（原 BUILDER 第一~六节）。
> 工单十一回报（待实测）仍在 BUILDER.md，此处只存已完结。

### 9.1 工单进度总表（原 BUILDER 第二节）

| 工单 | 状态 | commit | 备注 |
|---|---|---|---|
| 一：消息协议建型 + 摘 @ts-nocheck | ✅ 完成 | f278f2f | src/protocol.ts；四条验收全过 |
| 二：piClient 加固 | ✅ 完成 | eb81981 | 超时/EPIPE/piVersion 三处；实测三态 |
| 二b：残留收口 | ✅ 完成 | 59c473a | 三项全完，验收对账见 9.5 |
| 三：toolDetail 双实现统一 | ✅ 完成 | 4842557 | src/toolDetail.ts；用例已入库可复验 |
| 五：RPC 遗产清理 | ✅ 完成 | d22c3a3 / (并入 ce8521b) / 78287ac / 6eb8244 | 四项全完，结论见下 |
| 四：panel.ts 核心/宿主分层接线 | ✅ 完成已验收 | b981fe1 / e04a1dd / 00c13e2 | 死链删/对账移植/接线三笔 |
| 六：压缩显性化 + 上下文阈值预警 | ✅ 完成（待用户实测） | 4d89841 | 产品线第一单，回报见 9.3 |
| 七：pi 变更 Git diff 可视化 | ✅ 完成（待用户实测） | 191231f | 混合方案按裁决 11，回报见 9.2 |
| 十：dbg 日志上限/轮转 + importSession 大小上限 | ✅ 完成 | 10ef229 + 7158af5 | 见 BUILDER 原 〇-10 / 归档工单十 |

工单五逐项结论：①反向漂移观察断言已加（d22c3a3，只记日志零行为变化；观察期满零触发
后删两处对账纠偏）；②4s 兑底保留——扩展 registerCommand 集合开放无法枚举拦截
（结论注释落在 prompt 分支，注释实体被 ce8521b 并库）；③steer 自愈保留——正常路径
不可达，唯一可达场景是镜像漂移，作为最后一层防线（78287ac）；④ship 尾段改
scripts/publish.js 托管，失败红字非零退出（6eb8244）

总监 review 结论（截至工单三前）：477f8a1=8/10、9e46343=9/10、e87a411=满分。

### 9.2 工单七施工回报（191231f，混合方案按裁决 11）+ 第 0 步查证记录

**实现**：
- **piCore（核心，零 vscode）**：run 期间从 tool_execution_start 提取 edit/write 的
  args.path（裁决 4 保守检查），tool_execution_end 累积 edit 的 result.details.patch
  （jsdiff unified，按时间序）；agent_start/agent_settled 经公开钩子 onRunStart/onRunSettled
  中性回调通知 adapter；清单会话域持有：agent_start 清空、换 sessionFile 清空，多标签落地
  时直接复用为归属层
- **panel（adapter，git/diff/还原语义全在此）**：agent_start 时 git status --porcelain -z
  快照（-z 避开 quotepath 中文转义；rename 两段解析）；settled 时与现 status 比对：
  工具命中（source:tool）∪ 快照外新出现路径（source:git，bash 改动兜底）=「本次改动」。
  入口：banner 同款样式独立条（不动工单六横幅状态机），「查看改动」→ 文件 QuickPick
  （来源/可还原性/运行前已有改动标注）→ 动作二级选：查看 diff（vscode.diff，HEAD 版本经
  pi-head TextDocumentContentProvider，git show 实时取）或还原（模态二次确认
  deleteSession 同规格 + 脏缓冲提醒）
- **还原安全边界（裁决 11②③）**：git-only 只展示不可还原；工具命中 tracked →
  git checkout HEAD --；未跟踪/无 git 仅 edit 有 patch → patchRevert.ts 逆序逆向
  （任一行不符即拒打不落盘）；write 碰过的未跟踪文件一律不可还原。还原后从清单移除并刷新
- **协议三处同笔**：HostToWebview +changesList（files 空=收条）；WebviewToHost
  +showChanges/changesDismiss（经 UiActions 路由，架构不变）；PiEvent tool_execution_end
  result 补 details?（保守可选）。webview ES5 var 只渲染，changesbar 复用 banner CSS
- **patchRevert.ts**：纯字符串逆向还原器（BOM/EOL 处理与 pi edit.js 同款），
  12 断言用例入库（scripts/patchRevert.test.mts，jsdiff 借 pi 包树生成真实 patch
  做往返验证），`npm run test:revert` 可跑；不进 tsc 门禁（裁决 7 先例）

**验收对账**：compile 全绿 ✓；webview --noEmit 零报错（无 @ts-nocheck）✓；
patchRevert 12/12 ✓（含篡改拒打/越界拒打/乱内容拒打三组破坏性用例）；协议三处同步
（changesList/showChanges/changesDismiss 四文件 grep 均在位）✓；piCore 零 vscode 实 import ✓；
vsix 已打包（0.0.80 覆盖），out/patchRevert.js 在包 + 打包产物关键字段冒烟 PASS ✓

**待用户装包实测**（验收标准原文）：pi 改多文件 → diff 列表 → 单文件还原 → 内容确实回滚。
建议场景：①让 pi 正常编辑 2-3 个文件，settled 后看出条/看清单/看 diff；②还原一个 tracked
文件验证内容回滚；③未跟踪新文件上让 pi edit 后还原（patch 逆向路径）；④非 git 目录降级
（纯清单 + edit 可还原、write 不可还原提示）

**已知盲区（如实挂账）**：①run 期间用户手改的文件会被 git 兜底计入（source:git 标注，
不可还原——正是裁决 11② 的意图）；②非 git 目录下 bash 改动不可见（pi 自身不追踪，
如实降级）；③staged 新增（A 状态）按「不在 HEAD」处理走 patch 逆向路径；④diff 视图仅
tracked 文件提供（未跟踪无基线），patch 文本查看未做（等反馈）

**边界遵守**：横幅状态机零触碰（changesbar 是独立 DOM/CSS）；单 run 粒度未做多 run 聚合；
piClient.ts 零触碰；发现接线遗漏漂移：无

**第 0 步查证记录（施工前存档，结论已进裁决 11）**：

1. **事件流无 file-change 类事件**：AgentEvent 全集（pi-agent-core dist/types.d.ts:377-421）
   = agent_start/end、turn_start/end、message_start/update/end、tool_execution_start/update/end，
   共 11 种；AgentSessionEvent 扩展（pi-coding-agent dist/core/agent-session.d.ts:40-102）
   全部查过，无文件变更语义。docs/sdk.md Events 节同样无。命令响应（getMessages/getState）也不含。
2. **工具事件自带目标路径**：tool_execution_start {toolCallId, toolName, args}；edit/write
   的 schema 强制 args.path（dist/core/tools/edit.d.ts、write.d.ts）——流式期间即可知道 pi
   正在改哪个文件。edit 的 tool_execution_end result 还带 EditToolDetails {diff, patch}
   （现成 diff，还原/展示可用）。
3. **pi 官方口径就是「工具调用提取」**：compaction/utils 的 extractFileOpsFromMessage 按
   toolCall name∈{read,write,edit} 提取 args.path 汇总 modifiedFiles——pi 自己给压缩摘要
   用的就是这套，bash/powershell 改动 pi 同样不追踪。该函数未从包顶层导出，需自复刻
   （约 20 行，逻辑简单）。
4. **会话条目事后提取可行但不必**：SessionMessageEntry 持久化了 toolCall+result，
   理论上可从 session JSONL 事后扫，但内存事件流直接拿，无需读盘。

**选型建议：混合（A 为主、B 兜底）**：piCore 复刻 compaction 提取逻辑收集路径（中性消息
runChanges）；adapter git 快照比对兜底 bash 改动；未跟踪新文件的「还原」语义待总监定夺
（已由裁决 11 定：仅 edit 可逆向还原，write 不可）→ 已被裁决 11 批准并执行。

### 9.3 工单六施工回报（产品线第一单，4d89841）

**pi 事件事实（本单新考）**：pi AgentSession 事件流有 `compaction_start/end`
，reason=manual/threshold/overflow；end 带 result/aborted/willRetry/errorMessage。
此前落 piCore default 分支静默。自动压缩=reason 非 manual。

**实现**：
- 协议三处同笔：PiEvent +2 事件（result 保守 unknown，裁决 4）；HostToWebview +banner
  （BannerPayload，null=收起）；WebviewToHost +bannerClose/compactSession
- 横幅状态机全在 piCore：单份持有、变更才下发（setBanner 内 JSON 相等去重）；
  webviewReady 握手重发，webview 重建不丢
- 自动压缩成功 → 「↺ 上下文已自动压缩 · HH:MM」；失败（errorMessage 且非
  aborted/willRetry）透传 notice——压缩失败不可见，后续请求会莫名超限
- 阈值预警：refreshState 里 pct ≥ piChat.contextWarnPercent（默认 70，caps 读取）→
  预警横幅带一键压缩；涨破只提醒一次（armed 闸门），占比回落/换会话 re-arm；
  换 sessionFile 清会话域横幅（会话域信息不残留）
- webview 只渲染：header 下 #banner 单行条，关闭/一键压缩上报宿主；ES5 var 风格保持；
  CSS 用 /* */ 注释（// 会整段失效的老坑）
- 一键压缩走 ui.compactSession() 直压（810f39c 口径）；手动压缩 compactDone 通知不变

**验收对账**：compile 全绿 ✓；webview --noEmit 零报错 ✓（无 @ts-nocheck）；协议三处同步 ✓；
vsix 已打包（0.0.80 覆盖，package 不升版本）✓；编译产物 grep banner/compaction_end/
contextWarnPercent 全在位 ✓。

**待用户实测三场景**：①长对话触发自动压缩看横幅 ②阈值预警（可临时把
contextWarnPercent 调低如 5 验证）+ 一键压缩 ③横幅手动关闭 + webview 重建后不重发

**边界遵守**：状态机既有标志零触碰；手动 compact 入口未动；进度条未做（等反馈）；
未发现接线遗漏漂移。

**挂账**：compaction_start 未消费（「正在压缩…」即时反馈，等实测反馈再议）；
auto_retry_end 之类 willRetry 中间态的压缩重试最终失败只能看到最后一次 errorMessage。

### 9.4 工单四施工回报（接线专项，b981fe1 / e04a1dd / 00c13e2）

**对账基线（工单四-1）**：piCore 方法体移植自 **55e977b 的 panel.ts**——证据：
`git diff e562731~1..55e977b` 对 panel/protocol/piClient/i18n/webview 五文件为空，
而 piCore 随 e562731 扫库入库。以 `git diff 55e977b..HEAD -- src/panel.ts` 为权威
漂移清单逐 hunk 归属：

| 漂移 | 归属 | 处置 |
|---|---|---|
| ce8521b 原生斜杠命令表 + tuiOnly + 首 token 匹配 + / 补全去重 | piCore（拦截必须在乐观置 busy 前） | e04a1dd 已搬，run 目标经 UiActions |
| 810f39c /compact 直压 | 接口归 piCore，方法体留 adapter | UiActions 增 compactSession/exportSession/cloneSession |
| 2e773b3 插话计时 elapsedMs | piCore | e04a1dd 已搬 |
| fc134df 徽标回退 "⚡ Auto" | piCore | e04a1dd 已搬 |
| d22c3a3 反向 MISMATCH 观察断言 | piCore | e04a1dd 已搬 |
| 4781f78 删除入口 | adapter（纯 vscode UI） | 不搬，仍在 pickSession/deleteSessionPick |
| buildSettingsItems sMode 改 pickModeMenu | adapter | 不搬 |

piCore 相对基线的无出处偏差一并清理：onExit dbg 文本对齐 panel；
handleUiRequest select 包 {label} 与 newSession 确认框走 showConfirm 为等价改写（记录在案）。

**接线（工单四-2，00c13e2）**：panel.ts 2252→1338 行，实现 HostCapabilities（buildCaps）+
UiActions（buildUi）组装 PiCore；piCore 新增 currentSessionFile/hasCodeContext 只读口、
setModeText 写入口，onWebviewMessage/refreshState/handleUiRequest 转 public。

**行为零变化验证**：①核心消息发射点（`type: "..."`）与 HEAD 的 panel 逐类型 diff **完全一致**；
②grep piCore 零 vscode 实 import、panel 零核心状态残留；③compile 全绿 + webview --noEmit 零报错。

**踩坑（脚本手术三连）**：panel 手术用 python 锚点脚本，三次翻车均被断言/编译器拦住：
①newSession 删除范围误伤夹在中间的 pickSession（从 HEAD 提取+替换后插回）；
②nativeSlashCommands/sendSlashCommands/sendWorkspaceFiles/openSessionFile 忘删
（搬核心≠删 panel，grep `private async` 对账才抓全）；③edit 工具与脚本混用时
edit 原子性救场（锚点不匹配整组不落盘）。教训：大范围删方法前先 `grep -n "private"`
列全清单再定范围，范围边界用「下一个仍保留方法的注释行」而非「目标方法之后的第一个注释」。

**待验收**：UI 行为零变化是编译层/结构层结论，建议用户装 vsix 过一遍冒烟
（发消息/插队/新会话// 命令面板/权限徽标/历史切换）。

### 9.5 工单二b 施工回报（59c473a）+ 耗时功能（a28cee5，用户直接指令）

**工单二b 施工回报**：
1. **sessionList 死链删除**：panel.ts case + deleteSession 内调用 + sendSessionList()
   + protocol.ts 两类型整链删除；protocol 留裁决注释。零行为变化（webview 本无处理器）
2. **RPC 响应收口**：protocol.ts 新增 GetMessagesResult/GetStateResult/GetSessionStatsResult
   （复用 SessionMessage，字段全保守可选 + 索引签名），piClient 三方法改返回型；
   panel.ts 4 处 `(m: any)` + `stats: any` 清零
3. **用例入库**：scripts/toolDetail.test.mts（.mts 避免 ESM/CJS 纠缠），27 断言 7 组，
   `npm run test:detail` 零依赖可跑；.vscodeignore 已排除 scripts/ 不进 vsix（6b319b6）

**验收对账**：compile 全绿 ✓；sessionList/listSessions 消息链零残留（仅剩宿主本地 fs 函数
listSessions() 供 QuickPick 用、protocol 注释、用例字面量）✓；`(m: any)` 零残留 ✓；用例可跑 ✓

**意外收获（已追认，裁决 5）**：RPC 建型抓到 StateMsg 漂移——宿主实际发
`sessionName/sessionFile: null`（清空显示），但协议声明 `?: string`，此前靠 `st: any` 蒙混。
已按真实契约改 `?: string | null`。

**耗时功能（a28cee5，用户直接指令，非工单，已追认裁决 6）**：
- **口径决策**：宿主侧测量（agent_start→agent_settled），随 `busy:false` 下发 `elapsedMs`；
  中断也算一轮（时长到中断为止）；命令式应答无 agent 运行不带字段、webview 不误显示。
  webview 侧乐观计数含 1~2s 排队空窗，只做实时反馈，最终数字以宿主为准
- **协议同步**：BusyMsg.elapsedMs 可选字段，protocol.ts / panel.ts / webview 三处同笔提交
- **显示**：Working… 右侧实时秒数；settled 后状态栏 `⏱ 1m23s` 留显至下次状态变化
- **验证**：compile 全绿；已打 vsix（pi-for-vscode-0.0.75.vsix）

### 9.6 原 BUILDER「一、当前状态」历史流水（em 旧）

- **面板原生斜杠命令表（ce8521b，用户直令体验改进）**：输入框 /命令分三类——
  ① 面板原生 15 个（/model /thinking /theme /mode /new /resume /fork /tree /import /share
  /export /compact /clone /login /settings）本地执行不碰 prompt（prompt 对未注册 /xxx
  把字面文本发给模型，对扩展命令零 agent 事件假忙 4s）；② 终端专用 13 个拦截提示去终端
  （原先漏给模型）；③ 技能/模板正常走 prompt。命令表 nativeSlashCommands() 一处维护，
  输入框拦截与 / 补全面板共用，同名扩展命令（/mode）去重以原生为准
- **/mode 假忙修复（b040fb2，被 ce8521b 并入命令表）**：tuiCmds 拦截 /mode →
  本地 pickModeMenu()，设置菜单 sMode 项同步改掉
- **FEATURES.md 已退役（553f97f，用户删文件我摘引用）**：功能展示以 README 为唯一门面
- **直连版包级验证**：vsix 内零 node_modules（pi 未携带）、piSdk.js/piClient.js 在包、
  工单四半成品已排除；README 新口径已入包；vsix 内 out/ 重跑端到端 PASS
- **架构变更（55e977b，用户直令）**：piClient 从 spawn `pi --mode rpc` 子进程换为进程内
  AgentSession 直连（详见 总监.md 运营沉淀「架构与版本沿革」）；RPC 版整树保留分支
  `rpc-subprocess`（1d1f5ce）
- **工单四半成品（未跟踪）**：src/piCore.ts + src/hostCapabilities.ts，另一会话开了头未接线
  （已由工单四接线完成）
- **插话计时重置修复（2e773b3，直连后用户实测发现）**：prompt 乐观置位的 busy:true 在
  wasBusy=true 时漏带 elapsedMs（adcaaef 唯一漏网点），插话打回计时起点；已补
- 门禁：`npm run compile` 全绿；`tsc -p webview --noEmit`（无 @ts-nocheck）零报错

### 9.7 原 BUILDER「三、技术债 + 六、给总监的提醒」已销项

已销项（随对应工单/后续工作解决）：同步 IO 异步化（→工单十三）、状态机收敛（→排队）、
dbg 轮转（→工单十）、deleteSession 死链（→工单二b 删除）、耗时功能（→已追认）、
0.0.75/0.0.76 版本提醒（→0.0.87 前早已追平）。

### 9.8 原 BUILDER「四、关键决策与教训」（交接必读，仍有效）

1. **PiUnknownEvent 不并入 PiEvent 联合**：`type: string` 成员会毒化判别收窄（所有 case 属性
   访问塌成 `{}`）。default 分支用显式断言取错误字段。（裁决 2）
2. **webview/main.ts 曾有 @ts-nocheck**：源于它是字符串数组时代的逐字符忠实还原（工单一已摘帽，
   57 个错误全部清零，其中 48 个是同类 TS2554 可选参数）
3. **Windows 杀进程语义**：shell:true 下 `p.kill()` 只杀 cmd.exe 壳、不杀 pi 本体——
   piClient.dispose() 用 `taskkill /T /F` 是唯一正确杀法；测试时 `p.kill()` 后进程可能仍在服务
4. **pi RPC 事实**（docs/rpc.md 查证）：prompt 必回 response（接受/排队/拒绝都回，后续失败走事件流）
   —— send() 30s 超时因此安全；pi 退出时 close 处理器会 reject 全部 pending
5. **两个"别删"的兜底都有事故背景**：steering 自愈（busy 竞态 → 自动转 steer）、4s pendingPrompt
   （命令式应答不触发 agent_start）。详见 AGENTS.md 不可破坏约定
6. **vsix 曾 2.16MB**：截图打包是主因。现 README 截图走 GitHub raw URL，media 只留两个小图标
7. **编辑器红线不可信**：TS Server 多 tsconfig 布局下经常状态错乱，冲突时以 `tsc -p ./` 为准
8. **并行会话共用工作区，git add -A 必互扫**（工单五实测两次）：提交前必须 `git diff`
   逐 hunk 确认归属，只把属于自己的 hunk `git apply --cached` 进暂存区；发现扫错
   用 reset + hunk 拆分重做，工作区内容（对方的 WIP）不可丢（裁决 9）
9. **验证带副作用的脚本绝不直跑**：publish.js 验证时应改用 `git push --dry-run` 或
   临时远端；直跑导致 origin/main 被推上三个未受指示的提交

### 9.9 原 BUILDER「五、待总监回复的请示」存档

1. ~~PiUnknownEvent 不并入联合~~ 已裁决：认可（裁决 2）
2. ~~sessionList 死链~~ 已裁决：删整条链（工单二b 已执行）
3. ~~DIRECTOR.md 补记~~ 已裁决：摘帽实测 57 错记录在案
4. ~~StateMsg 字段改 `?: string | null`~~ 已裁决：追认（裁决 5）
5. ~~耗时功能~~ 已裁决：追认（裁决 6）
6. ~~scripts/toolDetail.test.mts 不进 tsc 门禁~~ 已裁决：接受现状（裁决 7）
7. ~~push 事故善后~~ 已裁决：用户发话 ship，推送保留、发版链路闭环——0.0.80 已 publish
8. **并行会话共用工作区的 git add -A 互扫**：已裁决（沉淀 AGENTS.md 5aca0f0，裁决 9）
9. ~~**工单七第 0 步选型请示**~~ 已裁决（裁决 11）并执行完毕（191231f），回报见 9.2
10. ~~**patchRevert.ts 不进 tsc 门禁、jsdiff 借 pi 包树**~~ 已裁决：沿用裁决 7 先例（工单七验收通过）