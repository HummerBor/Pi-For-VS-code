# 交接：启动慢战役（2026-09-23，用户叫停收工）

---

## 八、下半场战果（2026-09-23 接任后，已入库未 push）

> 本文开头的「一句话现状」已过时，以本节为准：启动秒开、列表秒出、Working 表不归零、
> 启动中发消息已验；正主仍未归案但残值只剩后台耗时（不挡人）。代码层新增冒烟/对照
> 两道测试门禁（smoke-boot 5 判据、test:listfast 4 判据），主链路改动不过测不装机。

- A 刀：启动首帧不等 pi init（postUiState 双闸解除 + fastRenderMsgs 兑底）→ 秒开
- R′刀：会话列表同步快扫（src/sessionScan.ts）→ 38s 变秒出，pi listAll 仍是后台权威
- A′刀：Working 计时起点改发送时刻（agent_start 归零 bug 结案）
- P 刀：发消息上屏 30~220ms 实测定罪；残值=启动渲染窗口期 ~1s（刀口：粘性回显，未出）
- Z2/Z3 探针判决：慢不认位置/文件，认服务闸；sync 读免疫；UV=32 毙掉线程池大小论
- 探针全部清场；UV 实验机上残留已清（注册表/脚本）
- **正主归案手段只剩**：Process Monitor 抓 Code.exe 读 ~/.pi 的 IRP（实验 19）——收益递减，未追

---

## （以下为接任时原文，部分结论已被上节更新）

> 单独交接文档（用户要求，不进 BUILDER.md）。接手前通读本文即可，无需回看对话。
> 一句话现状：**启动 14s → 2~6s、历史秒出、页签秒开、发送排队自动送出；正主 bug（扩展宿主读 `~\.pi` 文件慢 2~8s）未归案；Z 刀持久缓存两次炸点已整体回滚。**

---

## 一、现在的 bug（按优先级）

1. **【正主，未归案】扩展宿主（Code.exe）读 `~/.pi/agent` 下的 json/jsonl 文件慢 2~8 秒/个**
   - 同时刻 `node` 裸进程读同样文件：**毫秒级**。
   - 受害面：启动秒表走 2~6s（pi 就绪等待）；「选择历史会话 → Loading sessions...」很久。
   - 完整事实与排除清单见 §五。
2. **【体验残值】启动秒表 2~6s**——`pi 启动中…就绪后即可发消息 Ns`。历史已秒出、发消息已排队，秒表只是「pi 就绪」尾巴，随 bug 1 解决而消失。
3. **【需复测】启动中发消息**——已修（V 刀，见 §二坑 6），但用户尚未确认复测通过。
4. **【旧债】G 闸门**：`gate drop types=[render]` 仍偶发（判据：日志再现即切闸门时序刀）。
5. **【旧债】多窗共写一个 debug log**：轮转 5MB 导致考古困难（grep 时务必用行首时间戳锚，见 §四）。
6. **【旧债，未修】会话目录大小写分裂**：`--d--work-...` 与 `--D--work-...` 两个化名目录并存
   （pi 的目录化名大小写原样保留，盘符 `D:`/`d:` 随启动入口漂移，session-manager.js:245）。
   影响：会话散两目录、pi -r 候选只扫一半。修法方向：传给 pi 的 cwd 盘符归一。
7. **【旧债】lastSpoke 聚焦**（D 刀后续）：重启聚焦应=「最后说话的页签」而非文件 mtime。

## 二、踩的坑（事故教训，按发生序）

1. **冷启动 15.7s 真凶（M 刀已修）**：`loadPiSdk` 动态 import 的是 pi 包库入口
   `dist/index.js`（884 文件模块图），冷缓存 15.7s/热 1.2s。修法=优先 `dist/bundle/index.js`
   （单文件 bundle，257ms，API 面零缺项；旧包无 bundle 自动回退）。终端 pi 快正因它跑 bundle。
2. **services 全树扫描付了 N 次（Q 刀已修）**：`createAgentSessionServices` = `resourceLoader.reload()`
   全树扫描（skills/扩展/提示模板/AGENTS）。每页签一个 PiClient = 各扫各的。修法=进程级按 cwd
   共享（Promise 缓存）+ 激活即预热。共享安全已核：模型选择在 session 上、`runtime.dispose`
   只 dispose session（agent-session-runtime.js:112/302），services 无 dispose。
3. **O 刀回滚教训**：services 缓存曾做在 createRuntime 回调内，用户体感「变慢」，当场回滚不争。
   教训：**体感回归先回滚再谈**。
4. **「启动期不该有的结构」（R 刀已修）**：起步 `continueRecent` 先接「历史最近一条会话」、
   boot 链再 `switchSession` 换目标 = 双重挂载。修法=用户拍板的按需加载：**历史直读会话文件
   渲染**（jsonl `type:"message"` 的 `entry.message` {role,content,timestamp} 与
   `session.messages` 同形，探针核对过）+ `SessionManager.open(记忆路径)` 直连。
   配套修正两处：预热必须在 `postTabs()` 之后（快路径 render 同步发，早跑抢在页签栏前）；
   启动文案改自解释。
5. **Z 刀两次炸点（已整体回滚）**：
   - 炸点①：持久化回调里取 `this.wsKey()` → 运行时报 `this.wsKey is not a function`
     （this 形状随调用环境漂）。教训：**跨环境回调只捕获值，不捕获 this 行为**。
   - 炸点②：Memento 持久化把 `Date` 存成字符串，回读 `.getTime()` 炸。教训：**跨持久化
     边界的数据，类型复活是义务不是可选**。
   - 且两个炸点之外也没解决根因 → 三连 revert 整刀回滚。教训：**主链路 UI 改动没自测不装机**。
6. **「发消息被挡」（V 刀已修，待复测）**：日志零留痕 = 消息死在 webview 端。两处吞噬点：
   ①「输入框已画、按键未绑」死窗口（修=input 模板级 disabled，绑完按键才启用）；
   ②联想框开着时 Enter 一律 return（空列表/未选中也吞）（修=有选中项才应用建议，否则落回发送）。
7. **版本号口径（用户拍板，已入 AGENTS.md）**：ship 跟线上版本线；本地 package 号随便。
   曾误报「版本回退事故」——**看到版本变小/跳号不用排查**。
8. **考古工具坑（排查方法论）**：
   - debug log 5MB 轮转 + 多窗交错 + 会话正文里含我自己的 grep 命令文本 → **grep 必须行首
     时间戳锚**（`^2026-.*Z (事件名)`），否则命中我自己会话的正文洪水；
   - 多文件 grep 的 tail 会抓到轮转件尾部（真新鲜数据在活日志里，分开查）；
   - bash 吃 `/fo`、反斜杠（Windows）→ 用 `cmd //c` 或正斜杠；
   - 管道吞退出码曾把坏构建装上机（`npm run package | tail` 的退出码是 sed 的）→ **打包链
     必须看 "Packaged:" 行是否真出现**。
9. **瞎理论教训（用户原话「你瞎比比啥」）**：AV 扫描（两度）、事件洪水、async_hooks、
   线程池、工作区树、版本差……连环被实验枪毙。教训：**每个理论先设计可证伪实验再出口**。

## 三、做的测试（实验 → 结果 → 结论）

| # | 实验 | 结果 | 结论 |
|---|---|---|---|
| 1 | 探针 probe-boot-timing：loadPiSdk 冷/热 | 冷 15720ms / 热 1201ms | 884 文件模块图冷载（→M 刀） |
| 2 | bundle/index.js vs 库入口 | 433ms vs 1276ms，API 零缺项 | 换 bundle 安全 |
| 3 | 裸进程 services（本地 0.85.1 / 全局 0.87.0） | 81~177ms 双版本都快 | 版本差无罪 |
| 4 | create vs continueRecent（临时目录/真实目录） | 86~189ms | 双重挂载非耗时主因（结构仍改） |
| 5 | probe-env-contention：闲/事件洪水/async_hooks | 87/92ms | 洪水、hooks 无罪 |
| 6 | listAll 各会话目录 | 小目录 10~255ms，207MB 目录 1168ms | 目录大小有影响但非 10s 级 |
| 7 | services ∥ listAll 并行 | 合计 1168ms | 并行陪绑无罪 |
| 8 | S 刀 bench（loop/fs 计时）进扩展宿主 | loop 4~178ms、fs 0ms | JS 线程无罪（X 刀补充） |
| 9 | 工厂四行裸进程掐表（①create②ctor③reload④refresh） | 77/4/89/32ms | 四行裸进程全快 |
| 10 | T 刀：①注入拆表进扩展宿主 | ①=2769ms，③④≈100ms | 慢在 ModelRuntime.create |
| 11 | U 刀：`refreshOnCreate:false` | 28ms 一度生效 | 后续又见 1.5~3.9s = 慢不止刷新块 |
| 12 | X 刀：create 期间 150ms tick 连续采样 | tickMaxLag=956ms、gaps=1、heap+28MB | **异步干等**（同步卡死/GC 无罪） |
| 13 | Y 刀：外部资源逐个异步掐表 | auth=2642ms、models=5726ms、sessfile=2246ms；目录/stat/扩展自身文件全毫秒 | **点名：读 ~/.pi 下 json/jsonl 慢，带位置选择性** |
| 14 | UV_THREADPOOL_SIZE=2 +30 并发 stat | services 192ms | 线程池无罪 |
| 15 | 满池（6 并发 MB 级大读）下 services | 169ms | 线程池再毙 |
| 16 | fltmc / driverquery / tasklist | Defender 服务 STOPPED、无第三方 AV | 杀软无罪 |
| 17 | Windows Search 排除 `~\.pi` + 重启（用户实测） | 「没软用」 | 索引器无罪 |
| 18 | 慢读文件复制到别处对照 | **未做** | ← 剩余方向之一 |
| 19 | Process Monitor 看 Code.exe 读 auth.json 在等什么 | **未做** | ← 剩余方向之二 |

## 四、埋的点（全部在机，日志=~/.pi/agent/pi-chat-debug.log，5MB 轮转 .log.1）

**grep 姿势**：`grep -aE "^2026-.*Z (\[boot\]|boot |in |out )" <log>`（防命中会话正文）。

| 标记 | 含义 | 出处 |
|---|---|---|
| `boot start tab=T` / `boot done tab=T total=Nms` | 页签启动链首尾 | A 刀+分段 |
| `boot phase=ready/switch/model-mem/state/render +Nms` | boot 链五跳累计 | N 刀 |
| `[boot] load-pkg/runtime/init-done +Nms` | piClient init 三跳 | N 刀 |
| `[boot] services +Nms (单跳Nms[ 复用])` / `[boot] session …` | createRuntime 两段 | T 刀 |
| `[boot] bench pre/post loop=Nms fs=Nms` | 事件循环/同步 fs 探针 | S 刀 |
| `[boot] bench mr-create=Nms tickMaxLag=Nms gaps=N heapDelta=NMB` | create 拆表+同步卡死/干等判据+GC 旁证 | T/X 刀 |
| `[boot] tick lag=Nms` | create 期间 >200ms 的慢 tick | X 刀 |
| `[boot] io auth/models/store/sessdir/onesessdir/sessfile=Nms` | 外部资源逐个点名（累计值！单个=相邻差） | Y 刀 |
| `[boot] chunks-preload=Nms count=N` | bundle 懒 chunk 全量预载 | W 刀 |
| `in …`/`out …`/`gate drop` | A 刀全消息留痕 + 闸门丢弃 | A 刀 |
| `pickSession 占位弹出/缓存秒开/后台刷新就绪` | 选择器路径判读 | 工单十三/十九 |
| UI「pi 启动中…就绪后即可发消息 Ns」 | 启动秒表（用户点名加的） | N 刀 |

**探针脚本**：`scripts/probe-boot-timing.mjs`（包加载/服务创建计时）、
`scripts/probe-env-contention.mjs`（闲/洪水/hooks 环境差对照）。
**源码刀口**：`piClient.ts` getSharedServices/prewarmPiServices（Q/T/U/W/X/Y 全在这）、
`piCore.ts` boot 链分段+历史快路径（N/R）、`panel.ts` webviewReady 预热时序（R 修正）、
`piSdk.ts` bundle 入口（M）、`webview/main.ts` 启动秒表+发送吞噬点（N/V）。

**日志判读语义**：`[boot] io` 各行是**自 t0 累计**，单个资源耗时=相邻两行相减；
`bench pre` 在 services 前、`post` 在后；tick 断口=同步卡死，tick 连续而 await 不回=异步干等。

## 五、未归案根因（只写事实）

**事实**：扩展宿主读 `~/.pi/agent` 下 json/jsonl **每个 2~8s**（auth 2.6s / models 5.7s /
sessfile 2.3s，实测）；同时刻 node 裸进程**毫秒级**；同进程读扩展自身文件、目录 readdir、
statSync **全部毫秒级**（位置选择性！）。X 刀实锤**异步干等**（不是同步卡死/GC/线程）。

**已排除**（全部有实验）：版本差（0.85.1/0.87.0 双版本裸进程都快）、网络/代理（无配置、
refresh 已 allowNetwork:false）、Defender（服务已停）、第三方 AV（无）、Windows Search
（用户加排除+重启无效）、libuv 线程池（满池复现不出）、async_hooks、事件洪水、JS 线程卡顿、
GC、会话目录大小、工作区树大小、并行扫描陪绑。

**剩余方向**（接任者优先做这两个对照，别再猜）：
1. **文件换位置对照**：把一个慢读文件（auth.json）复制到 `%TEMP%` 读 vs 原位读——同进程同时刻
   各 10 次。原位慢/副本快 = 路径/文件属性相关（ADS、加密位、备份软件盯目录）；一样慢 = 进程级。
2. **Process Monitor**：抓 Code.exe 读 auth.json 那次 IRP，看 Duration 和它在等哪个驱动/操作。

## 六、环境事实（本机，接手不必重查）

- Defender 实时防护**停**；无第三方 AV；Windows Search 已排除 `~\.pi`（无效）。
- 会话目录大小写分裂（`--d--`/`--D--` 双目录，见 §一.6）。
- pi 包：开发仓 node_modules=0.85.1，用户全局=0.87.0（`C:\Program Files\nodejs\node_modules\@earendil-works\pi-coding-agent`，bundle/index.js + chunks/ 52 个懒 chunk）。
- 版本号口径/审计红线/`--no-session` 禁令等约定见 AGENTS.md（不再重复）。
- git 状态：Z 刀三连提交已三连 revert；HEAD 含 Q~Y 刀+回滚+V 刀全量；只做本地 commit 未 push。

## 七、用户情绪与协作教训（重要）

用户反馈原话：「花了快 30 块了 小米模型 非常垃圾的体验」「你瞎比比啥」「该解决的问题没解决
还出了新问题」。教训：**少而稳**——理论先有可证伪实验再出口；主链路改动没自测不装机；
一次只让用户提供一个读数；报错类问题当场修不隔夜。下一步建议先做 §五 两个对照实验归案
根因，别再动功能。
